// Re-export the playground page as the default export for the index route
// This allows accessing the playground at /uniform-playground/
import { PlaygroundPage } from "./pattern-playground";

export default PlaygroundPage;
